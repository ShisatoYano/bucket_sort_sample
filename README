Bucket Sort
============

* Bucket sort is mainly useful when input is uniformaly distributed over a range.  
* Time Complexity of sorting individual bucketes using insertion sort is O(n).  
* Concatenating all sorted buckets also takes O(n) time.  
